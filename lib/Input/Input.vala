using SDL.Input;

namespace Virgil.Input {
    public KeyCode ord (string key_string) {
        switch (key_string) {
            case "NULL":
                return KeyCode.UNKNOWN;
        }
    }
}