/*
namespace Virgil.Engine {
    public static void LoadPNG (string gresource_file = "default.png") {
        GLib.Resource gres = new GLib.Resource.load (gresource_file);

        GLib.InputStream stream = gres.open_stream (gresource_file, GLib.ResourceLookupFlags.NONE);
    }
}
*/
